<Window x:Class="TextEditor.Views.SaveView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save form" Height="150" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBox Margin="15" Width="250" FontSize="17" Text="{Binding Path=DocumentName}">
                </TextBox>
                    <Button Content="Confirm" Margin="15" FontSize="17" Command="{Binding ConfirmCommand}">
                </Button>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Please, input name of document" FontSize="17" Margin="5">
                </Label>
        </StackPanel>
    </Grid>
</Window>
